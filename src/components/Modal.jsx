import { useGlobalContext } from "../context";

const Modal = () => {
  return <asid className="modal-overlay">
    <div className="modal-container">
      <h1>Modal container</h1>
    </div>
  </asid>
}

export default Modal